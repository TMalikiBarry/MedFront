import {AppPositiveNumberDirective} from './app-positive-number.directive';

describe('AppPositiveNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new AppPositiveNumberDirective();
    expect(directive).toBeTruthy();
  });
});
